const loginForm = document.querySelector(".login-from");
const IdInput = loginForm.querySelector("input:first-child");
const PassInput = loginForm.querySelector("input:nth-child(2)");

loginForm.addEventListener("submit", (event) => {});
